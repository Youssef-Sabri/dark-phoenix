# setuptools<81 / wheel are pinned here so they're present in the runtime env.
# The same pin is ALSO enforced at wheel-BUILD time via PIP_CONSTRAINT in
# main.py, because pip builds in isolated envs that a plain requirement line
# can't reach. setuptools>=81 removed pkg_resources, which whisperx@v3.2.0 and
# transitive deps need. See m-bain/whisperX#1210.
setuptools<81
wheel
Cython
av
tqdm
# torch is pre-installed by Hugging Face ZeroGPU runtime environment;
# we let HF manage torch to ensure CUDA compatibility with the RTX Pro 6000 Blackwell
opencv-python-headless
torchvision
ffmpegcv
numpy<2.0
python_speech_features
scipy
scenedetect==0.5.6.1
scikit-learn
gdown
pandas
transformers
accelerate
datasets
google-genai
pysubs2
boto3
fastapi[standard]
gradio>=5.0.0
spaces
pyannote-audio==3.1.1
faster-whisper==1.0.0
ctranslate2>=4.5.0
nvidia-cudnn-cu12
nltk
yt-dlp
bgutil-ytdlp-pot-provider
